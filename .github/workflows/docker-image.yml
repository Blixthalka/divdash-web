name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repo
      uses: actions/checkout@v3

    - name: Build JS app
      uses: actions/setup-node@v1
      with:
        node-version: 14.x
      run: npm run build

    - name: Build the Docker image
      run: docker build . --tag divdash-web:$(date +%s)
